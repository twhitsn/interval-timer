'use strict';

document.addEventListener('DOMContentLoaded', function() {
    const controller = new Controller('timeDisplay', 'intervalForm', 'intervalContainer');
    controller.setAudio(document.getElementById('audio'));



    //var deck = new Deck();


}, false);
